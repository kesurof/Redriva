# Configuration Redriva - Real-Debrid Token
# ==========================================
# 
# Instructions:
# 1. Copiez ce fichier vers config/.env
# 2. Remplacez "votre_token_ici" par votre vrai token Real-Debrid
# 3. Gardez ce fichier secret (ne le partagez jamais)
#
# Obtenir votre token Real-Debrid:
# https://real-debrid.com/apitoken
#

# Token Real-Debrid (obligatoire)
RD_TOKEN=votre_token_ici

# Configuration optionnelle (valeurs par défaut optimisées)
RD_MAX_CONCURRENT=50
RD_BATCH_SIZE=250
RD_QUOTA_WAIT=60
RD_TORRENT_WAIT=10

# Configuration Flask (pour environnements Docker)
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=false

# Configuration Gunicorn (pour optimiser les performances en production)
GUNICORN_WORKERS=2
GUNICORN_TIMEOUT=120
GUNICORN_KEEPALIVE=5
# RD_PAGE_WAIT=1.0
